import { CanActivate,ExecutionContext,ForbiddenException,Injectable } from '@nestjs/common'; import { Reflector } from '@nestjs/core'; import { PERMISSIONS_KEY } from '../decorators/permissions.decorator';
@Injectable() export class PermissionsGuard implements CanActivate { constructor(private r:Reflector){} canActivate(c:ExecutionContext){const need=this.r.getAllAndOverride<string[]>(PERMISSIONS_KEY,[c.getHandler(),c.getClass()]);if(!need?.length)return true;const u=c.switchToHttp().getRequest<any>().user;if(u.roles?.includes('admin')||need.every((x:string)=>u.permissions?.includes(x)))return true;throw new ForbiddenException('Missing permission');}}
